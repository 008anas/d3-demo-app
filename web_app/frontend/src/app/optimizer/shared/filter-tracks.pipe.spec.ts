import { FilterTracksPipe } from './filter-tracks.pipe';

describe('FilterTracksPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTracksPipe();
    expect(pipe).toBeTruthy();
  });
});
